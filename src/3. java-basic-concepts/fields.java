/**
 * A small program that defines some fields.
 * Trying to create some illegal field names and see what kind of error the compiler produces.
 * And Using the naming rules and conventions as a guide.
 */
 
 
 public class fields {
 	public static void main(String[] args) {
		
		//Declaring & Initializing Variables
		
		int integer = 5;
		float floating = 5.0f;
		double floatingDouble = 5.0;
		String stringType = "This is a string type";
		byte byteType = 1;
		short shortType = 2;
		char character = 'A';
		boolean b = true;
		
		System.out.println("Interger:" + integer + "\nFloating(float):"
			+ floating + "\nfloating(double): " + floatingDouble 
			+ "\nString:" + stringType + "\nByte:" + byteType 
			+ "\nShort:" + shortType + "\nCharacter:" + character 
			+ "\nBoolean:" + b);
			
		}
	}
